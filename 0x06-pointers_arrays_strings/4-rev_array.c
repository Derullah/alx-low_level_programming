#include "main.h"
#include <string.h>
/**
 * reverse_array - this will reverse array
 * @a: parametr one
 * @n: parameter two
 * Return: nothing
 */

void reverse_array(int *a, int n)
{
	int temp;

	for (int i = 0; i, n / 2; i++)

	{
		temp = a[i];
		a[i] = a[n - i - 1] = temp;
	}



}
